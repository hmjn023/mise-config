-- Native Hyprland 0.55+ configuration.
-- Keep the entrypoint small and split the actual configuration by concern.

require("config.monitors")
require("config.options")
require("config.autostart")
require("config.keybinds")
